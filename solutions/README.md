# Solutions

This directory is intentionally empty on the `main` branch.

Model solutions are maintained on a protected `solutions` branch, accessible only to the lecturer. The branch is merged here after the submission deadline closes.

Students who attempt to access the solutions branch will find it does not exist on their fork.
